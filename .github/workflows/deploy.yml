name: Concurrency Workflow
on: 
    push:
    workflow_dispatch: 

jobs:
    job_1:
        runs-on: ubuntu-latest
        steps:
        - name: Deploying code
          run: echo "Deploy code"

        - name: sleep
          run: sleep 500